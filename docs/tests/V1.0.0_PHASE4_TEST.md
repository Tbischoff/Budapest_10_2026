# Phase 4 · Build 1
1. SQL aus `docs/V1.0.0_PHASE4_DATABASE.sql` ausführen.
2. Gerät A: neuen Ort hinzufügen; ohne Reload auf B prüfen.
3. Supabase: Eintrag in `places` und `trip_places` prüfen.
4. Gerät B: Ort bearbeiten; Live-Änderung auf A prüfen.
5. Gerät A: `Aus Reise entfernen`; Ort muss auf beiden Geräten verschwinden.
6. Supabase: `places` bleibt erhalten, `trip_places`-Zuordnung ist entfernt.
